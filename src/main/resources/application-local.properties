# Configuration for local development connecting to the Dockerized PostgreSQL database
spring.datasource.url=jdbc:postgresql://localhost:5432/etalente
spring.datasource.username=user
spring.datasource.password=password

# Ensure Flyway is enabled for the local profile
spring.flyway.enabled=true
